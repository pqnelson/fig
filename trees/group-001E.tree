\title{Cosets are equal iff product with inverse in subgroup}
\author{alex-nelson}
\import{notation}
\date{2024-01-06}
\taxon{theorem}
\MML{group_2}{thm 114}{T114}
\MML{group_2}{thm 120}{T120}

\p{Let #{G} be a group. Let #{H} be a subgroup of #{G}. Let #{a} and #{b} be elements of #{G}. Then:
\ol{
\li{#{a\cdot H=b\cdot H} if and only if #{b^{-1}\cdot a\in H}}
\li{#{H\cdot a=H\cdot b} if and only if #{b\cdot a^{-1}\in H}}
}}