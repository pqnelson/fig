\title{Associativity of multiplying subgroups by group elements}
\author{alex-nelson}
\import{notation}
\date{2024-01-06}
\taxon{theorem}

\p{Let #{G} be a group. Let #{H} be a subgroup of #{G}. Let #{a} and #{b} be elements of #{G}. Then the following all hold:}
\ol{
\li{#{(a\cdot b)\cdot H=a\cdot(b\cdot H)}}
\li{#{(a\cdot H)\cdot b=a\cdot(H\cdot b)}}
\li{#{(H\cdot a)\cdot b=H\cdot(a\cdot b)}}
}